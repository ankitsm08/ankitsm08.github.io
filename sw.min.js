self.addEventListener("install", t => { self.skipWaiting() }), self.addEventListener("activate", t => { self.registration.unregister().then(() => self.clients.matchAll()).then(t => { t.forEach(t => { t.url && "navigate" in t && t.navigate(t.url) }) }) });